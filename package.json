{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@11ty/eleventy-plugin-syntaxhighlight": "^4.1.0",
    "@changesets/cli": "^2.19.0",
    "husky": "^7.0.2",
    "prettier": "~2.2.1",
    "pretty-quick": "~3.1.0"
  },
  "scripts": {
    "prepare": "husky install",
    "build": "npm run build --workspaces --if-present",
    "build.web-comps": "npm run build.stencil --workspace=packages/web-components",
    "test": "npm run test --workspace=packages/web-components --workspace=packages/react",
    "web-comps.install": "npm ci --ignore-scripts --workspace=packages/web-components"
  },
  "engines": {
    "node": "^16",
    "npm": "~8.5"
  },
  "workspaces": [
    "packages/web-components",
    "packages/react",
    "packages/angular-workspace"
  ]
}
