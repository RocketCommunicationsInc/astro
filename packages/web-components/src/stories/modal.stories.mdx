import { extractArgTypes } from '@astrouxds/storybook-addon-docs-stencil'
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { html, render } from 'lit-html'

<Meta
    title="Components/Modal"
    component="rux-modal"
    argTypes={extractArgTypes('rux-modal')}
    parameters={{
        actions: {
            handles: ['ruxmodalclosed rux-modal'],
        },
    }}
/>

# Modal

A Modal interrupts the app experience to prompt a user to confirm an action or acknowledge a piece of information. The modal header, content and footer are exposed as slots, allowing control of what goes in them.

## Guidelines

-   [Astro UXDS: Modal](https://astrouxds.com/ui-components/dialog-box)

export const Default = (args) => {
    return html`
    <style>
        .sbdocs #story--components-modal--default-story {
        display: block;
        min-height: 500px;
    }
        rux-modal::part(modal-wrapper) {
            position: absolute;
            top: 0px;
            left: 0px;
            width: 100%;
            height: 100%;
        }
    </style>
        <div style="display: flex; flex-flow: column; justify-content: center;">
            <rux-modal id="default-modal" ?open="${args.open}">
                <h4 slot="header">Modal Title</h4>
                <div>
                    <rux-input
                        placeholder="Modal Input"
                        label="Modal Content"
                    ></rux-input>
                </div>
                <div
                    slot="footer"
                    style="width: 100%; display: flex; justify-content: space-between; align-items: center;"
                >
                    <div>
                        <a
                            href="#"
                            style="margin-right: 10px; text-decoration: none; color: var(--color-primary);"
                            >Modal Link</a
                        >
                    </div>
                    <div>
                    <rux-button-group>
                        <rux-button id="cancel-btn" secondary>Cancel</rux-button>
                        <rux-button id="confirm-btn">Confirm</rux-button>
                    </div>
                </div>
            </rux-modal>
        </div>
    `
}

<Canvas>
    <Story
        args={{
            open: true,
        }}
        name="Default"
    >
        {Default.bind()}
    </Story>
</Canvas>

## API

<ArgsTable of="rux-modal" />

## Variants

### Close on click away

export const ClickToClose = (args) => {
    return html`
        <style>
            .sbdocs #story--components-modal--click-to-close {
                display: block;
                min-height: 500px;
            }
            rux-modal::part(modal-wrapper) {
                position: absolute;
                top: 0px;
                left: 0px;
                width: 100%;
                height: 100%;
            }
        </style>
        <div style="display: flex; flex-flow: column; justify-content: center;">
            <rux-modal
                ?open="${args.open}"
                ?click-to-close="${args.clickToClose}"
            >
                <h4 slot="header">Click Off Close</h4>
                <div>
                    <p>
                        If click-to-close is true, clicking off of rux-modal
                        will close it.
                    </p>
                </div>
                <div
                    slot="footer"
                    style="width: 100%; display: flex; justify-content: space-between; align-items: center;"
                >
                    <div>
                        <a
                            href="#"
                            style="margin-right: 10px; text-decoration: none; color: var(--color-primary);"
                            >Modal Link</a
                        >
                    </div>
                    <div>
                        <rux-button-group>
                            <rux-button secondary>Cancel</rux-button>
                            <rux-button>Confirm</rux-button>
                        </rux-button-group>
                    </div>
                </div>
            </rux-modal>
        </div>
    `
}

<Canvas>
    <Story
        args={{
            open: true,
            clickToClose: true,
        }}
        name="Click To Close"
    >
        {ClickToClose.bind()}
    </Story>
</Canvas>

## Opening, Closing and Focusing

Because the content of modal is now customiziable via slots, the actions of these slots will require some boilerplate to get working.

### Opening and Closing

The `open` prop of `rux-modal` is mutable and can be changed accordingly. Here is a basic example of doing so with buttons:

```js
<rux-modal id="modal" click-to-close>
    <h4 slot="header">Modal Header</h4>
    <p>Modal Content here!</p>
    <div slot="footer">
        <rux-button id="confirm">Confirm</rux-button>
    </div>
</rux-modal>
<script>
    const modal = document.getElementById('modal');
    const confirm = document.getElementById('confirm')
    confirm.addEventListener('click', () => {
        modal.open = false;
    })
</script>
```

### Applying Focus

Often times when a modal opens, you want to autofocus a specific element. Here's a quick way to do that
using the previous example:

```js
<rux-modal id="modal" click-to-close>
    <h4 slot="header">Modal Header</h4>
    <p>Modal Content here!</p>
    <div slot="footer">
        <rux-button id="confirm">Confirm</rux-button>
    </div>
</rux-modal>
<script>
    const modal = document.getElementById('modal');
    const confirm = document.getElementById('confirm')
    modal.addEventListener('onruxmodalopened', () => {
        setTimout(() => {
            confirm.focus()
        }, 0)
    })
    confirm.addEventListener('click', () => {
        modal.open = false;
    })
</script>
```

## Cherry Picking

If you're already utilizing a build system that supports tree shaking and want to only import this individual component:

```js
import RuxModal from '@astrouxds/astro-web-components/dist/components/rux-modal.js'
import RuxButton from '@astrouxds/astro-web-components/dist/components/rux-button.js'
customElements.define('rux-modal', RuxModal)
customElements.define('rux-button', RuxButton)
```
