import { extractArgTypes } from '@astrouxds/storybook-addon-docs-stencil'
import { Meta, Story, Canvas, ArgsTable } from '@storybook/blocks'
import { html, render } from 'lit-html'
import * as MenuItemStories from './menu-item.stories';

<Meta of={MenuItemStories} />

# Menu Item

Menu Item should be used inside of [Pop Up](?path=/docs/components-pop-up--default-story).

export const Default = (args) => {
    return html`
        <rux-menu-item
            ?disabled="${args.disabled}"
            .download="${args.download}"
            .href="${args.href}"
            .rel="${args.rel}"
            .target="${args.target}"
            .value="${args.value}"
            >Menu Item</rux-menu-item
        >
    `
}

<Canvas>
  <Story of={MenuItemStories.Default_} />
</Canvas>

## API

<ArgsTable of="rux-menu-item" />

## Variants

### Disabled

export const Disabled = (args) => {
    return html`
        <rux-menu-item
            ?disabled="${args.disabled}"
            .download="${args.download}"
            .href="${args.href}"
            .rel="${args.rel}"
            .target="${args.target}"
            .value="${args.value}"
            >Menu Item</rux-menu-item
        >
    `
}

<Canvas>
  <Story of={MenuItemStories.Disabled_} />
</Canvas>

## Cherry Picking

If you're already utilizing a build system that supports tree shaking and want to only import this individual component:

```js
import { RuxMenuItem } from '@astrouxds/astro-web-components/dist/components/rux-menu-item'
customElements.define('rux-menu-item', RuxMenuItem)
```
