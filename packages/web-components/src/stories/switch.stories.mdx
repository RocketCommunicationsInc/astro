import { extractArgTypes } from '@astrouxds/storybook-addon-docs-stencil'
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { html, render } from 'lit-html'

<Meta
    title="Components/Switch"
    component="rux-switch"
    argTypes={extractArgTypes('rux-switch')}
    parameters={{
        actions: {
            handles: [
                'rux-input rux-switch',
                'rux-change rux-switch',
                'rux-blur rux-switch',
            ],
        },
    }}
/>

# Switch

A Switch toggles between two mutually exclusive states such as "On" or "Off." Unlike a checkbox, a switch initiates an action with immediate effect without requiring a "Save" or "Submit" action.

## Guidelines

-   [Astro UXDS: Switch](http://www.astrouxds.com/ui-components/switch)

export const Switch = (args) => {
    return html`
        <div style="margin: 3rem auto; max-width: 5rem; text-align: center;">
            <rux-switch
                .disabled=${args.disabled}
                .checked=${args.checked}
                .errorText=${args.errorText}
                .helpText=${args.helpText}
                id="01"
            ></rux-switch>
        </div>
    `
}

<Canvas>
    <Story
        parameters={{
            docs: { source: { code: '<rux-switch></rux-switch>' } },
        }}
        name="Switch"
    >
        {Switch.bind()}
    </Story>
</Canvas>

## API

<ArgsTable of="rux-switch" />

## Variants

### Disabled

export const Disabled = (args) => {
    return html`
        <div style="margin: 3rem auto; max-width: 5rem; text-align: center;">
            <rux-switch
                .disabled=${args.disabled}
                .checked=${args.checked}
                .errorText=${args.errorText}
                .helpText=${args.helpText}
                id="01"
            ></rux-switch>
        </div>
    `
}

<Canvas>
    <Story
        name="Disabled"
        args={{
            disabled: true,
        }}
        parameters={{
            docs: { source: { code: '<rux-switch disabled></rux-switch>' } },
        }}
    >
        {Disabled.bind()}
    </Story>
</Canvas>

### Help Text

export const HelpText = (args) => {
    return html`
        <div style="margin: 3rem auto; max-width: 5rem; text-align: center;">
            <rux-switch
                .disabled=${args.disabled}
                .checked=${args.checked}
                .errorText=${args.errorText}
                .helpText=${args.helpText}
                id="01"
            ></rux-switch>
        </div>
    `
}

<Canvas>
    <Story
        name="HelpText"
        args={{
            helpText: 'Help Text',
        }}
        parameters={{
            docs: {
                source: {
                    code: '<rux-switch help-text="Help Text"></rux-switch>',
                },
            },
        }}
    >
        {HelpText.bind()}
    </Story>
</Canvas>

### Error Text

export const ErrorText = (args) => {
    return html`
        <div style="margin: 3rem auto; max-width: 5rem; text-align: center;">
            <rux-switch
                .disabled=${args.disabled}
                .checked=${args.checked}
                .errorText=${args.errorText}
                .helpText=${args.helpText}
                id="01"
            ></rux-switch>
        </div>
    `
}

<Canvas>
    <Story
        name="ErrorText"
        args={{
            errorText: 'Error',
        }}
        parameters={{
            docs: {
                source: {
                    code: '<rux-switch error-text="Error"></rux-switch>',
                },
            },
        }}
    >
        {ErrorText.bind()}
    </Story>
</Canvas>

## Cherry Picking

If you're already utilizing a build system that supports tree shaking and want to only import this individual component:

```js
import { RuxSwitch } from '@astrouxds/astro-web-components/dist/components/rux-switch'
customElements.define('rux-switch', RuxSwitch)
```
