import { Meta, Story, Canvas, Controls } from '@storybook/blocks';
import * as TagStories from './tag.stories';

<Meta of={TagStories} />

# Tag

Tags help users quickly identify important information related to an item and categorize items by keywords.

<Canvas of={TagStories.Default} />

## API

<Controls />

## Usage

### Valid Status

When using the status prop of `rux-tag`, make sure that the status is of a valid type: `pass`, `fail`, or `unknown`.
If the status isn't provided or isn't one of the above mentioned types, it will default to `unknown`.

## Variants

### All Variants

<Canvas of={TagStories.AllVariants} />

## Cherry Picking

If you're already utilizing a build system that supports tree shaking and want to only import this individual component:

```js
import { RuxTag } from '@astrouxds/astro-web-components/dist/components/rux-tag.js'
customElements.define('rux-tag', RuxTag)
```
