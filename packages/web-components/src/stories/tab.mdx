import { extractArgTypes } from '@astrouxds/storybook-addon-docs-stencil'
import { Meta, Story, Canvas, Controls } from '@storybook/blocks'
import { html, render } from 'lit-html'
import * as TabStories from './tab.stories';

<Meta of={TabStories} />

# Tab

Tab belongs to the Tabs component.

## Guidelines

* [Astro UXDS: Tabs](https://www.astrouxds.com/components/tabs/)

export const Default = (args) => {
    return html`
<rux-tab id="tab1"
    ?disabled=${args.disabled}
    ?selected=${args.selected}
>
    Tab 1
</rux-tab>
    `
}

<Canvas>
  <Story of={TabStories.Default_} />
</Canvas>

## API

<Controls />

## Variants

### Disabled

export const Disabled = (args) => {
    return html`
<rux-tab id="tab1"
    ?disabled=${args.disabled}
    ?selected=${args.selected}
>
    Tab 1
</rux-tab>
    `
}

<Canvas>
  <Story of={TabStories.Disabled_} />
</Canvas>

### Selected

export const Selected = (args) => {
    return html`
<rux-tab id="tab1" ?disabled=${args.disabled} ?selected=${args.selected}
    >Tab 1</rux-tab
>
    `
}

<Canvas>
  <Story of={TabStories.Selected_} />
</Canvas>

## Cherry Picking

If you're already utilizing a build system that supports tree shaking and want to only import this individual component:

```js
import { RuxTab } from '@astrouxds/astro-web-components/dist/components/rux-tab'
customElements.define('rux-tab', RuxTab)
```
