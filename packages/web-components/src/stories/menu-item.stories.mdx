import { extractArgTypes } from '@astrouxds/storybook-addon-docs-stencil'
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { html, render } from 'lit-html'

<Meta
    title="Components/Pop Up Menu/Menu Item"
    component="rux-menu-item"
    subcomponents={{ RuxPopUpMenu: 'rux-pop-up-menu' }}
    argTypes={extractArgTypes('rux-menu-item')}
    parameters={{
        actions: {
            handles: ['ruxmenuitemselected', 'rux-menu-item'],
        },
    }}
/>

# Menu Item

Menu Item should be used inside of [Pop Up Menu](?path=/docs/components-pop-up-menu--default-story)

export const Default = (args) => {
    return html`
        <rux-menu-item
            ?disabled="${args.disabled}"
            .download="${args.download}"
            .href="${args.href}"
            .rel="${args.rel}"
            .target="${args.target}"
            .value="${args.value}"
            >Menu Item</rux-menu-item
        >
    `
}

<Canvas>
    <Story
        name="Default"
        args={{
            disabled: false,
            download: "",
            href: "",
            rel: "",
            target: "",
            value: {}
        }}
    >
        {Default.bind()}
    </Story>
</Canvas>

## API

<ArgsTable of="rux-menu-item" />

## Variants

### Disabled

export const Disabled = (args) => {
    return html`
        <rux-menu-item
            ?disabled="${args.disabled}"
            .download="${args.download}"
            .href="${args.href}"
            .rel="${args.rel}"
            .target="${args.target}"
            .value="${args.value}"
            >Menu Item</rux-menu-item
        >
    `
}

<Canvas>
    <Story
        args={{
            disabled: true,
            download: "",
            href: "",
            rel: "",
            target: "",
            value: {}
        }}
        name="Disabled"
    >
        {Disabled.bind()}
    </Story>
</Canvas>

## Cherry Picking

If you're already utilizing a build system that supports tree shaking and want to only import this individual component:

```js
import RuxMenuItem from '@astrouxds/astro-web-components/dist/components/rux-menu-item'
customElements.define('rux-menu-item', RuxMenuItem)
```
