import { extractArgTypes } from '@astrouxds/storybook-addon-docs-stencil'
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import { html, render } from 'lit-html'
import { DepTag } from './storybook-tags.stories.mdx'
import { styled } from '@storybook/theming'



<Meta
    title="Components/Progress"
    component="rux-progress"
    argTypes={extractArgTypes('rux-progress')}
/>

# Progress

A visual indicator of a task's progress. When indicating progress with a finite duration, use determinate progress. When indicating progress with an indefinite duration, use indeterminate progress.

## Guidelines

-   [Astro UXDS: Progress](https://astrouxds.com/components/progress/)

export const Default = (args) => {
    return html`
<div style="margin: 3rem auto;  padding: 2rem; text-align: center;">
    <rux-progress
        value="${args.value}"
        .max=${args.max}
        ?hide-label="${args.hideLabel}"
    ></rux-progress>
</div>
    `
}

<Canvas>
    <Story
        args={{
            value: 1,
            hideLabel: false,
            max: 100
        }}
        name="Default"
    >
        {Default.bind()}
    </Story>
</Canvas>

## API

<ArgsTable of="rux-progress" />

## Usage

### Label

If `max` is 100, the label is displayed as a percentage.
If `max` is not 100, the label is displayed as value/max (1450/2000)

## Variants

### Determinate Progress Max

export const DeterminateProgressMax = (args) => {
    return html`
        <div style="margin: 3rem auto;  padding: 2rem; text-align: center;">
            <rux-progress
                value="${args.value}"
                max=${args.max}
                ?hide-label="${args.hideLabel}"
            ></rux-progress>
        </div>
    `
}

<Canvas>
    <Story
        args={{
            value: 100,
            max: 100,
            hideLabel: false
        }}
        name="Determinate Progress Max"
    >
        {DeterminateProgressMax.bind()}
    </Story>
</Canvas>

### Determinate Progress with Custom Max

export const DeterminateProgressCustomMax = (args) => {
    return html`
<div style="margin: 3rem auto;  padding: 2rem; text-align: center;">
    <rux-progress
        value="${args.value}"
        max=${args.max}
        ?hide-label="${args.hideLabel}"
    ></rux-progress>
</div>
    `
}

<Canvas>
    <Story
        args={{
            value: 1123,
            max: 2400,
            hideLabel: false
        }}
        name="Determinate Progress with Custom Max"
    >
        {DeterminateProgressCustomMax.bind()}
    </Story>
</Canvas>

## Indeterminate Progress <DepTag>DEPRECATED</DepTag>

A `<rux-progress>` without a value will render an indeterminate state with `<rux-indeterminate-progress>`. This is
being deprecated and that functionality will be removed in Astro 7.0. 

export const IndeterminateProgress = (args) => html`
<div style="padding: 5%">
    <rux-progress
    ></rux-progress>
</div>
`

<Canvas>
    <Story
        name="Indeterminate Progress"
        argTypes={{
            hideLabel: { table: { disable: true } },
            max: { table: { disable: true } },
            value: { table: { disable: true } }
        }}
    >
        {IndeterminateProgress.bind()}
    </Story>
</Canvas>

### Cherry Picking

If you're already utilizing a build system that supports tree shaking and want to only import this individual component:

```js
import { RuxProgress } from '@astrouxds/astro-web-components/dist/components/rux-progress'
customElements.define('rux-progress', RuxProgress)
```
