import { extractArgTypes } from '@astrouxds/storybook-addon-docs-stencil'
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { html, render } from 'lit-html'

<Meta
    title="Components/Timeline"
    component="rux-timeline"
    argTypes={extractArgTypes('rux-timeline')}
/>

# Timeline

## Guidelines

export const Default = (args) => {
    return html`
        <div style="width: 1050px; margin: auto">
            <rux-timeline zoom="${args.zoom}">
                <rux-track id="track" label="second">
                    <div slot="label">
                        <status></status>
                        Region1
                    </div>
                    <rux-time-region start="0000" end="0100">
                        <rux-status status="normal"></rux-status>
                        Region1
                    </rux-time-region>
                </rux-track>
                <rux-track label="second">
                    <div slot="label">Region2</div>
                    <rux-time-region start="0300" end="0400">
                        <rux-status status="normal"></rux-status>
                        Region4
                    </rux-time-region>
                    <rux-time-region start="0400" end="0500">
                        <rux-status status="normal"></rux-status>
                        Region5
                    </rux-time-region>
                    <rux-time-region start="0400" end="0500">
                        <rux-status status="normal"></rux-status>
                        Region5
                    </rux-time-region>
                    <rux-time-region start="0400" end="0500">
                        <rux-status status="normal"></rux-status>
                        Region5
                    </rux-time-region>
                </rux-track>
                <rux-track label="first">
                    <div slot="label">Region3</div>
                    <rux-time-region start="0100" end="0300">
                        <rux-status status="normal"></rux-status>
                        Region2
                    </rux-time-region>
                    <rux-time-region start="0200" end="0800">
                        <rux-status status="normal"></rux-status>
                        Region3
                    </rux-time-region>
                </rux-track>
                <rux-track label="third">
                    <div slot="label">Region3</div>
                    <rux-time-region start="0500" end="0600">
                        <rux-status status="normal"></rux-status>
                        Region6
                    </rux-time-region>
                    <rux-time-region start="0600" end="0700">
                        <rux-status status="normal"></rux-status>
                        Region6
                    </rux-time-region>
                </rux-track>
                <rux-track>
                    <rux-ruler></rux-ruler>
                </rux-track>
            </rux-timeline>
        </div>
    `
}

<Canvas>
    <Story
        name="Default"
        args={{
            zoom: 120,
        }}
    >
        {Default.bind()}
    </Story>
</Canvas>

## API

<ArgsTable of="rux-timeline" />

## Timeline

The Timeline component is responsible for

-   creating and manipulating the grid used to display events.
-   rendering the appropriate labels for **Ruler**

<Story name="Track">{''}</Story>

## Track

Tracks are responsible for containing individual **Time Regions** across a horizontal lane. They are composed of a header section and a time region section.

<ArgsTable of="rux-track" />

## Time Region

Time Regions represent single events in time. A `start` time and an `end` time are required. If a `start` and `end` time aren't provided, or an invalid range is provided,
the component will fail silently and not render. A valid `start` and `end` time must fall within the date range specified on the root **Timeline** component

```
<rux-time-region
    start="2021-02-01T00:00:00Z"
    end="2021-02-01T00:30:00Z"
>
    Region1
</rux-time-region>
```

<ArgsTable of="rux-time-region" />

## Ruler

**Ruler** is responsible for displaying the intervals of time across the timeline grid.

```
    <rux-ruler></rux-ruler>
```

<ArgsTable of="rux-ruler" />
