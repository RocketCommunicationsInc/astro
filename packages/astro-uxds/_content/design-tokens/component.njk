---
tags: resources
path: /design-tokens/component
date: Last Modified
layout: interior.template.njk
title: Light Theme Component Tokens
dark: true
---
<style>
	sl-tab-group {
		--indicator-color: var(--secondary-color);
	}
	sl-tab[active]::part(base) {
		color: var(--secondary-color);
	}
	sl-tab:hover::part(base) {
		color: var(--secondary-color);
	}
</style>

<h1>Component Tokens</h1>

<sl-tab-group>
	<sl-tab slot="nav" panel="dark">Dark Theme</sl-tab>
	<sl-tab slot="nav" panel="light">Light Theme</sl-tab>

	<sl-tab-panel name="dark">
		<h2>Classification Banner</h2>

{% for token in tokens.component('light', 'classification-banner') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Notification Banner</h2>

{% for token in tokens.component('light', 'notification-banner') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Status Symbol</h2>

{% for token in tokens.component('light', 'status-symbol') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Slider</h2>

{% for token in tokens.component('light', 'slider') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Global Status Bar</h2>

{% for token in tokens.component('light', 'gsb') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Switch</h2>

{% for token in tokens.component('light', 'switch') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Radio</h2>

{% for token in tokens.component('light', 'radio') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Checkbox</h2>

{% for token in tokens.component('light', 'checkbox') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Tag</h2>

{% for token in tokens.component('light', 'tag') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Scrollbar</h2>

{% for token in tokens.component('light', 'scrollbar') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Progress</h2>

{% for token in tokens.component('light', 'progress') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Indeterminate Progress</h2>

{% for token in tokens.component('light', 'indeterminate-progress') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Dialog</h2>

{% for token in tokens.component('light', 'dialog') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Card</h2>

{% for token in tokens.component('light', 'card') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Container</h2>

{% for token in tokens.component('light', 'container') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

	</sl-tab-panel>

	<sl-tab-panel name="light">


<h2>Classification Banner</h2>

{% for token in tokens.component('light', 'classification-banner') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Notification Banner</h2>

{% for token in tokens.component('light', 'notification-banner') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Status Symbol</h2>

{% for token in tokens.component('light', 'status-symbol') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}
<hr/>

<h2>Slider</h2>

{% for token in tokens.component('light', 'slider') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Global Status Bar</h2>

{% for token in tokens.component('light', 'gsb') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Switch</h2>

{% for token in tokens.component('light', 'switch') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Radio</h2>

{% for token in tokens.component('light', 'radio') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Checkbox</h2>

{% for token in tokens.component('light', 'checkbox') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Tag</h2>

{% for token in tokens.component('light', 'tag') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Scrollbar</h2>

{% for token in tokens.component('light', 'scrollbar') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Progress</h2>

{% for token in tokens.component('light', 'progress') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Indeterminate Progress</h2>

{% for token in tokens.component('light', 'indeterminate-progress') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Dialog</h2>

{% for token in tokens.component('light', 'dialog') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Card</h2>

{% for token in tokens.component('light', 'card') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

<hr/>

<h2>Container</h2>

{% for token in tokens.component('light', 'container') %}
	<rux-design-token-preview
		name="{{ token.name }}"
		value="{{ token.value }}"
		alias="{{ token.referenceToken }}"
		description="{{ token.description }}"
		type="{{ tokens.lookupProperty(token.category, token.property) }}"
	>
	</rux-design-token-preview>
{% endfor %}

	</sl-tab-panel>
</sl-tab-group>

