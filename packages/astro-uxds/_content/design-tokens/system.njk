---
tags: resources
path: /design-tokens/system
date: Last Modified
layout: interior.template.njk
title: Dark Theme System Tokens
dark: true
---
<style>
	sl-tab-group {
		--indicator-color: var(--secondary-color);
	}
	sl-tab[active]::part(base) {
		color: var(--secondary-color);
	}
	sl-tab:hover::part(base) {
		color: var(--secondary-color);
	}

</style>

<h1>System Tokens</h1>

<sl-tab-group>
	<sl-tab slot="nav" panel="dark">Dark Theme</sl-tab>
	<sl-tab slot="nav" panel="light">Light Theme</sl-tab>

	<sl-tab-panel name="dark">
		<h2>Background Colors</h2>

		{% for token in tokens.system('dark', 'color', 'background') %}
			<rux-design-token-preview
				name="{{ token.name }}"
				value="{{ token.value }}"
				alias="{{ token.referenceToken }}"
				description="{{ token.description }}"
				type="background"
			></rux-design-token-preview>
		{% endfor %}

		<hr/>

		<h2>Text Colors</h2>

		{% for token in tokens.system('dark', 'color', 'text') %}
			<rux-design-token-preview
				name="{{ token.name }}"
				value="{{ token.value }}"
				alias="{{ token.referenceToken }}"
				description="{{ token.description }}"
				type="text"
			></rux-design-token-preview>
		{% endfor %}

		<hr/>

		<h2>Border Colors</h2>

		{% for token in tokens.system('dark', 'color', 'border') %}
			<rux-design-token-preview
				name="{{ token.name }}"
				value="{{ token.value }}"
				alias="{{ token.referenceToken }}"
				description="{{ token.description }}"
				type="border"
			></rux-design-token-preview>
		{% endfor %}

		<hr/>

		<h2>Status Colors</h2>

		{% for token in tokens.system('dark', 'color', 'status') %}
			<rux-design-token-preview
				name="{{ token.name }}"
				value="{{ token.value }}"
				alias="{{ token.referenceToken }}"
				description="{{ token.description }}"
				type="background"
			></rux-design-token-preview>
		{% endfor %}

		<hr/>

		<h2>Classification Colors</h2>

		{% for token in tokens.system('dark', 'color', 'classification') %}
			<rux-design-token-preview
				name="{{ token.name }}"
				value="{{ token.value }}"
				alias="{{ token.referenceToken }}"
				description="{{ token.description }}"
				type="background"
			></rux-design-token-preview>
		{% endfor %}

	</sl-tab-panel>

	<sl-tab-panel name="light">
		<h2>Background Colors</h2>

		{% for token in tokens.system('light', 'color', 'background') %}
			<rux-design-token-preview
				name="{{ token.name }}"
				value="{{ token.value }}"
				alias="{{ token.referenceToken }}"
				description="{{ token.description }}"
				type="background"
			></rux-design-token-preview>
		{% endfor %}

		<hr/>

		<h2>Text Colors</h2>

		{% for token in tokens.system('light', 'color', 'text') %}
			<rux-design-token-preview
				name="{{ token.name }}"
				value="{{ token.value }}"
				alias="{{ token.referenceToken }}"
				description="{{ token.description }}"
				type="text"
			></rux-design-token-preview>
		{% endfor %}

		<hr/>

		<h2>Border Colors</h2>

		{% for token in tokens.system('light', 'color', 'border') %}
			<rux-design-token-preview
				name="{{ token.name }}"
				value="{{ token.value }}"
				alias="{{ token.referenceToken }}"
				description="{{ token.description }}"
				type="border"
			></rux-design-token-preview>
		{% endfor %}

		<hr/>

		<h2>Status Colors</h2>

		{% for token in tokens.system('light', 'color', 'status') %}
			<rux-design-token-preview
				name="{{ token.name }}"
				value="{{ token.value }}"
				alias="{{ token.referenceToken }}"
				description="{{ token.description }}"
				type="background"
			></rux-design-token-preview>
		{% endfor %}

		<hr/>

		<h2>Classification Colors</h2>

		{% for token in tokens.system('light', 'color', 'classification') %}
			<rux-design-token-preview
				name="{{ token.name }}"
				value="{{ token.value }}"
				alias="{{ token.referenceToken }}"
				description="{{ token.description }}"
				type="background"
			></rux-design-token-preview>
		{% endfor %}

	</sl-tab-panel>
</sl-tab-group>